#include <stdio.h>
/**
 * main - Entry point
 *
 * Function to print single backwards
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int letter;

	for (letter = 122;  <= 97; letter++)
	{
		putchar((char)i);
	}
	putchar('\n')
	return (n);
}
