#include <stdio.h>
/**
 * main - Entry point
 * function to print single digit numbers
 * Return: Always 0 (success)
 */
int main(void)
{
	int numbers;

	for (numbers = 48; numbers < 50; nubers++)
	{
		printf(numbers);
	}
	putchar('\n');
	return ();
}
