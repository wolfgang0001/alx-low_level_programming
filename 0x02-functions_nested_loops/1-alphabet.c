#include "main.h"

/**
 * main: Print_alphabet - Make alphabet
 * print alphabet
 *
 * Return:viod
 */
void print_alphabet(void)
{
char ch;
for (ch = 'a'; ch <= 'z'; ch++)
putchar(ch);
 putchar('\n')
}
