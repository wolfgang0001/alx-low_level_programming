#include "main.h"

/**
 * _abs - runs the value of an absolute integer
 * @n: the int to check
 * Return: the absolute value of integer
 */
int _abs(int n)
{
	if (n >= 0)
	{
	return (n);
	}
	return (-n);
}
